{
  "name": "feed-curator-for-reddit",
  "version": "1.1.0",
  "description": "Firefox extension that filters unwanted posts out of Reddit feeds by category, subreddit, pattern and keyword. Entirely local.",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:chrome": "node scripts/build.mjs --target=chrome",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live": "node scripts/live-smoke.mjs",
    "lint:ext": "web-ext lint --source-dir dist",
    "package": "npm run build && web-ext build --source-dir dist --artifacts-dir web-ext-artifacts --filename feed-curator-for-reddit-{version}.xpi --overwrite-dest",
    "package:chrome": "npm run build:chrome && web-ext build --source-dir dist-chrome --artifacts-dir web-ext-artifacts --filename feed-curator-for-reddit-chrome-{version}.zip --overwrite-dest",
    "package:source": "mkdir -p web-ext-artifacts && git archive --format=zip -o web-ext-artifacts/feed-curator-for-reddit-source.zip HEAD",
    "start:firefox": "web-ext run --source-dir dist --start-url https://www.reddit.com/r/popular/",
    "check": "npm run typecheck && npm test && npm run build && npm run lint:ext && npm run build:chrome"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "esbuild": "^0.28.2",
    "jsdom": "^29.1.1",
    "puppeteer-core": "^25.12.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "web-ext": "^10.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/deanyo/Curddit.git"
  }
}
